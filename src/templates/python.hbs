"""
@package {{#if isAbstract}}Abstract {{/if}}{{getFullName}}
"""
class {{getFullName}}{{#if getExtends}}({{#with getExtends}}{{getFullName}}{{/with}}){{/if}}:
    def __init__(self):
        """
        Constructor for {{getFullName}}
        """{{#if hasFields}}{{#each getFields}}
        self.{{this.getName}} = None{{/each}}
{{else}}

        pass
{{/if}}

{{#each getMethods}}
    def {{#if this.isPrivate}}_{{/if}}{{this.getName}}{{#if this.getParameters}}(self, {{#each this.getParameters}}{{#if @first}}{{else}},{{/if}}{{#if this.getName}}{{this.getName}}{{else}}param{{@index}}{{/if}}{{/each}}){{else}}(self){{/if}}:
        """
        """
    {{#if this.needsReturnStatement}}
        return null
{{else}}
        pass
{{/if}}

{{/each}}
