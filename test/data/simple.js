// 3rd party modules
const Query = require('Query');
const A = require('A');
const B = require('B');

/**
 * Class Hep
 */
class Hep {
  /**
   * Constructor for Hep
   */
  constructor() {
  }

  /**
   * @param { A } a TBD
   * @param { B } b TBD
   */
  test(a, b) {
    // TBD
  }

  /**
   * @param { A } a TBD
   */
  find(a) {
    return Query;
  }
}

module.exports = Hep;
